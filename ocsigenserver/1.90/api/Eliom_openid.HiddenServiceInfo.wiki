=Module type <<a_api_type | module type Eliom_openid.HiddenServiceInfo >> =
<<div class="code"|<<span class="keyword"|module type>> HiddenServiceInfo = <<span class='code'|sig>><<a_api text=".." | module type Eliom_openid.HiddenServiceInfo >><<span class='code'|end>>>>~Information about the hidden service\\

~This functor build a hidden service that will be used
    to receive the remote server~'s data~. ~In return
    you get a check function\\

----
<<div id="VALpath" class="code"|<<span class="keyword"|val>> path : <<span class="code type"|string list>>>><<div class="info"|~The path of the hidden service\\
>>
<<div id="VALf" class="code"|<<span class="keyword"|val>> f : <<span class="code type"|(string * string) list -> unit -> Eliom_output.Any.page Lwt.t>>>><<div class="info"|~The function called when an user connects to the hidden service
    ~(not that hidden~) without being in an identication process~.
    ~Typically you should redirect the user to the login page~.\\
>>
